# Scalable Microservice - Product Management API

A basic microservice built with Node.js and Express that manages product inventory.

---

## 📋 Features
- CRUD operations (Create, Read, Update, Delete)
- Health check endpoint
- Filter products by price range
- JSON response format
